<view class="page"><text class="eyebrow">第 1 步 / 共 3 步</text><text class="title">确认图片后，开始 AI 识别。</text><text class="copy">图片会发送至你配置的 AI 服务进行识别与解答。</text><view class="card"><text class="label">待识别图片</text><image wx:if="{{imagePath}}" class="confirm-preview" src="{{imagePath}}" mode="aspectFit"/><text wx:else class="card-copy">暂未找到图片，请返回上一步重新选择。</text></view><view class="card"><text class="card-title">隐私提示</text><text class="card-copy">请不要上传含姓名、学校或考号的图片。</text></view><view wx:if="{{loading}}" class="thinking-card"><view class="spinner"></view><text class="thinking-title">AI 正在解题</text><text class="thinking-copy">{{status}}</text><text class="thinking-time">已思考 {{elapsed}} 秒</text></view><button class="primary" loading="{{loading}}" disabled="{{loading}}" bindtap="next">{{loading ? '正在生成解析…' : '开始 AI 解题'}}</button></view>
