# AI 学习助手（暂定名）开发环境与部署准备

> 目标：先在 Windows 本机完成可用的九年级数学学习 Demo，同时从项目起步就保留平滑上线到微信小程序与云服务的能力。

## 1. 总体原则

第一阶段不处理微信审核、支付、正式域名和云服务器。开发环境仍需遵守以下原则：

1. 学生端页面、后端 AI 服务和数据存储分离；
2. 本地与线上只替换配置，不重写业务代码；
3. AI 密钥永远只保存在后端环境变量中；
4. 图片文件不直接存入数据库；
5. 所有可变配置均使用环境变量；
6. 不在第一阶段引入微服务、消息队列、多个数据库或 Kubernetes。

## 2. 推荐本地开发环境

```text
Windows 本机
├─ VS Code：编写代码
├─ Git：版本管理
├─ Node.js + pnpm：运行前端、后端和构建工具
├─ 微信开发者工具：预览、调试和真机测试小程序
├─ Docker Desktop + WSL 2：本地运行 PostgreSQL 等服务
├─ PostgreSQL：保存业务数据
└─ AI API：仅由本地后端读取密钥并调用
```

### 2.1 必装软件

| 软件 | 推荐用途 | 说明 |
| --- | --- | --- |
| VS Code | 代码编辑器 | 可安装中文语言包、ESLint、Prettier 等基础扩展 |
| Git | 版本管理 | 保存开发历史，并为后续 GitHub/Gitee/服务器部署做准备 |
| Node.js 22 LTS | JavaScript/TypeScript 运行环境 | 同时用于 Taro 学生端和 Node.js 后端 |
| pnpm | 依赖管理工具 | 适合一个仓库内包含多个应用和共享包的结构 |
| 微信开发者工具 | 小程序调试 | 导入小程序构建结果、调试和真机预览 |
| Docker Desktop | 本地容器环境 | 通过 WSL 2 运行 PostgreSQL，部署时可复用配置 |

### 2.2 第一版可暂缓的软件与服务

- Redis：后续有任务队列、缓存、限流需求时再增加；
- Elasticsearch：题库规模较小时不需要；
- Kubernetes：第一阶段完全不需要；
- 原生 Android/iOS 开发环境：暂不制作原生 App；
- PDF/Word 讲义导出环境：教师讲义功能阶段再加入；
- 正式云服务器、对象存储、备案域名：本地 Demo 验证后再准备。

## 3. 项目技术选型

| 模块 | 第一阶段建议 | 选择原因 |
| --- | --- | --- |
| 学生端 | Taro + React + TypeScript | 可同时构建微信小程序与 H5，便于复用业务代码 |
| 教师端 | 暂不开发；后续独立 Web | 教师需要更适合电脑端的编辑、筛选和导出界面 |
| 后端 | Node.js + Fastify 或 NestJS | 与前端共用 TypeScript，适合图片、AI 调用和业务接口 |
| 数据库 | PostgreSQL | 适合用户、题目、知识点、练习和学习记录之间的关联数据 |
| 数据库访问 | Prisma | 数据模型清楚，后续迁移与维护方便 |
| 图片开发期存储 | 本地 `data/uploads/` | 不依赖云服务，便于本地调试 |
| 图片上线后存储 | 腾讯云 COS 或 S3 兼容对象存储 | 可替换、易扩展，不与应用服务器磁盘绑定 |
| 公式数据 | LaTeX 源码 | 小程序和 Web 可以使用不同渲染器，但共享同一份公式数据 |
| AI 接入 | 独立 AI 网关模块 | 便于更换模型供应商、记录成本、统一校验输出 |

## 4. 建议的目录结构

```text
AI_Learning_Companion/
├─ apps/
│  ├─ student/               学生端：Taro，输出微信小程序与 H5
│  └─ api/                   后端：认证、图片、AI、学习记录
├─ packages/
│  └─ shared/                前后端共用类型、接口契约、知识点定义
├─ infra/
│  └─ docker-compose.yml     PostgreSQL 等本地服务与未来部署配置
├─ docs/                     产品、接口和运维文档
├─ data/                     本地上传图片等运行数据，不提交 Git
├─ .env.example              可提交的配置模板，不含任何真实密钥
├─ .env.local                本机私密配置，不提交 Git
├─ .gitignore
└─ README.md
```

后续教师端加入时，新增 `apps/teacher-web/`，不影响学生端和后端的既有结构。

## 5. 本地运行关系

```text
微信开发者工具 / 真机预览
            ↓
学生端 Taro 开发服务
            ↓
本地后端 API
      ├──────→ 本地图片目录 data/uploads/
      ├──────→ Docker 中的 PostgreSQL
      └──────→ 外部 AI 服务
```

### 5.1 各层职责

- **学生端**：拍照、选图、确认题干、展示答案和解析、提交练习与反馈。
- **后端**：用户身份、图片处理、调用 AI、校验输出、保存学习数据、后续计费。
- **数据库**：用户、知识点、题目、AI 解答、练习、作答和掌握情况。
- **图片目录/对象存储**：原图、裁切图等文件；数据库只保存文件地址、状态和元数据。
- **AI 网关**：集中管理模型选择、提示词、请求日志、耗时、成本、重试与输出结构校验。

## 6. 配置与安全要求

### 6.1 环境变量

项目至少区分以下环境：

- `development`：个人电脑本地开发；
- `test`：后续用于少量用户体验和联调；
- `production`：正式上线环境。

需要使用环境变量管理的内容包括：

- 数据库连接地址；
- AI 服务地址、模型名和 API 密钥；
- 图片存储方式与访问地址；
- 后端服务地址；
- 日志级别；
- 后续的微信 AppID、商户号等配置。

`.env.example` 只写变量名称和示例值；`.env.local` 写真实值，并必须加入 `.gitignore`。

### 6.2 AI 密钥与学生数据

- 任何 AI API 密钥不得写入小程序代码、网页代码、Git 仓库或截图；
- 小程序只向自己的后端上传图片和请求；
- 后端调用 AI 后只返回需要展示的结果；
- 题目图片可能出现姓名、学校、班级、考号等信息，前端应提示裁切非必要信息；
- 开发期使用的真实试卷和学生数据也应控制访问范围，并提供删除能力。

## 7. 为未来上线预留的设计

本地 Demo 成熟后，部署不应改变业务结构，只做以下替换：

| 本地开发 | 正式部署 |
| --- | --- |
| `localhost` 后端地址 | HTTPS 域名与正式 API 地址 |
| Docker 中本地 PostgreSQL | 云服务器或托管 PostgreSQL |
| `data/uploads/` | 对象存储（例如 COS） |
| `.env.local` | 云端环境变量/密钥管理 |
| 开发日志 | 正式日志、监控与告警 |
| 微信开发者工具预览 | 上传版本、审核与发布 |

因此，后端代码需要从第一天就通过“文件存储服务接口”访问图片，而不是把电脑硬盘路径写死在业务逻辑中。

## 8. 第一阶段搭建顺序

1. 检查并安装 Git、Node.js、pnpm、微信开发者工具、Docker Desktop；
2. 初始化 Git 仓库与目录结构；
3. 创建 Taro 学生端项目；
4. 创建 Node.js 后端项目；
5. 通过 Docker Compose 启动 PostgreSQL；
6. 建立 `.env.example`、`.env.local` 和 `.gitignore`；
7. 实现一个健康检查接口和小程序首页，确认前后端可以通信；
8. 实现图片上传；
9. 再接入 AI 解题、LaTeX 渲染和学习记录；
10. 最后实现“一道同类练习 + 掌握反馈”的 MVP 闭环。

## 9. 第一阶段完成时应具备的能力

- 本机一条命令能启动数据库、后端和学生端开发环境；
- 微信开发者工具可以预览学生端；
- 小程序可上传一张题目图片到本地后端；
- 后端可安全调用 AI，并返回结构化结果；
- 公式以 LaTeX 存储并能正常展示；
- 用户、题目、知识点和学习反馈可写入 PostgreSQL；
- 所有密钥和本地数据都不进入 Git；
- 后续只需替换环境变量、数据库与图片存储，即可进入测试或正式部署准备。
